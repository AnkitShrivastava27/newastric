import { useEffect } from 'react';
import { Link } from 'react-router-dom';
import './Support.css';
const FAQS = [{ q: 'Is Astric available right now?', a: 'The project is positioned as an early-access product. The site is designed to collect interest and guide users into the launch flow.' }, { q: 'What kind of AI does Astric use?', a: 'Astric is structured around a business-first AI assistant that answers with the context available inside the app.' }, { q: 'Can the app generate websites?', a: 'Yes. The product pages describe website generation as part of the AI workflow, including previewing and iterating drafts.' }, { q: 'How do plans work?', a: 'The experience is organized into clear tiers so teams can start small and grow into more capable plans later.' }, { q: 'How do I contact support?', a: 'Use the contact page to send a message. The form is already wired to the project’s backend route.' }, { q: 'Can I give feedback about the design?', a: 'Yes. The product is still evolving, so feedback on usability, copy, and launch flow is welcome.' }];
export default function Support() { useEffect(() => { const observer = new IntersectionObserver((entries) => entries.forEach((entry) => entry.isIntersecting && entry.target.classList.add('visible')), { threshold: 0.12 }); document.querySelectorAll('.reveal').forEach((el) => observer.observe(el)); return () => observer.disconnect(); }, []); return (<div className="support-page"><section className="page-hero"><div className="container"><div className="badge animate-fade-up"><span>✦</span> Support</div><h1 className="animate-fade-up" style={{ animationDelay: '0.08s' }}>Answers that are<br /><span className="gold-text">easy to find.</span></h1><p className="page-hero-sub animate-fade-up" style={{ animationDelay: '0.16s' }}>A compact help hub for common launch and product questions.</p></div></section><section className="section"><div className="container faq-grid">{FAQS.map((item, index) => (<article key={index} className="card faq-card reveal" style={{ transitionDelay: `${index * 0.06}s` }}><h4>{item.q}</h4><p>{item.a}</p></article>))}</div></section><section className="section"><div className="container support-actions reveal"><div><p className="section-label">Need something else?</p><h2>Send the team a message.</h2><p>Use the contact form for bugs, feedback, or access requests.</p></div><Link to="/contact" className="btn btn-gold">Contact support</Link></div></section></div>); }
